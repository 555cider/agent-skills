interface:
  display_name: "Agent Memory"
  short_description: "Authoritative indexed memory shared by Codex, Claude, OpenCode, and other coding agents."
  default_prompt: "Use $agent-memory when prior preferences, repo facts, decisions, handoffs, or remember/update requests may affect the task. Recall prompt-relevant project memory first; include global memory only for trusted repos. Capture durable non-sensitive learnings, preserve promoted bodies and evidence, and version changed facts instead of overwriting history."
