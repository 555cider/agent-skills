interface:
  display_name: "Plan Graph"
  short_description: "Route work through persistent plan context."
  default_prompt: "Use $plan-graph to route the current task and changed paths through .agents/plans, review prerequisites and affected plans, then maintain the smallest valid plan set with the lifecycle CLI."
